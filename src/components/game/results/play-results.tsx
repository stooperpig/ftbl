import './play-results.css';

interface PropTypes {
}

export const PlayResultsPanel = (props: PropTypes) => {
    return(
        <div className="play-results-panel">
            PlayResults
        </div>
    );
}
//import { sendMessage } from '../../../api/web-socket';
//import { CLOSE_MODAL } from '../../../types/action-constants';
//import { AppDispatch } from '../../../constants/store';
//import { Action, CloseModalAction } from '../../../types/action-types';

export const closeModal = (sync: boolean, payload: any) => {
};